#!/bin/bash

PATH=/usr/local/bin:/usr/bin:/bin

apt-get update

puppet \
  apply \
  --verbose \
  --logdest=/var/log/puppet.log \
  --noop 
  --modulepath=/home/<%= @username %>/.puppet/modules/puppet-nginx-php-fpm-mysql/modules /home/<%= @username %>/.puppet/modules/puppet-nginx-php-fpm-mysql/manifests/site.pp
